
// This is 1D array.

#include<stdio.h>

int main()
{
    // int marks[]={10,20,30,40,50};  /*  we do not gives index size but we gives number of data for index size and
                                        // it take 5 index size automatically.  */
    int marks[5]; // we gives 5 index in this array for take input.

    // int marks[5]={10,20,30,40,50}; // We gives 5 index size and data for 5 index value. 

    printf("Enter the marks");
    marks[0] =10;
    return 0;
}
