// 1. Write a program in C to display the first 10 natural numbers.

#include <stdio.h>

int main()

{
    printf("The first 10 natural number = ");
    for (int i = 1; i <= 10; i++)
    {
        printf(" %d,", i);
    };
    return 0;
}
